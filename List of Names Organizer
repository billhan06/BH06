#this is where the user can type in the students names
students_names = []
name_list = ""
for count in range (0,6):
	if count == 0:
		name_list = "first"
	elif count == 1:
		name_list = "second"
	elif count == 2:
		name_list = "third"
	elif count == 3:
		name_list = "forth"
	elif count == 4:
		name_list = "fifth"
	elif count == 5:
		name_list = "sixth"
#this is where the loop is just with the wording altered a bit
	students_names.append(input(f"Enter the {name_list} name: "))
print(students_names)
#the students names are put into alphabetical order
